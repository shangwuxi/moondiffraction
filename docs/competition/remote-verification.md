# Remote verification (initial passing snapshot)

Date: 2026-09-11. Public repository: https://github.com/shangwuxi/moondiffraction . Owner and process-scoped authenticated actor: shangwuxi (account id 316128219). No global Git identity or stored active-account switch was performed.

Initial remote main SHA bda12d2cd2a1a05df5a8079e7492159e317664cc matched the uploaded local graph. GitHub commit API maps both author and committer to shangwuxi; contributors API returned only shangwuxi, 22 contributions. Later documentation/release commits may increase raw count, but qualifying development count remains conservatively 20.

All four CI jobs (wasm-gc/wasm/js/native) passed check/build/test, three examples, 496-case independent oracle and generated API freshness. Evidence: https://github.com/shangwuxi/moondiffraction/actions/runs/34609667244 and evidence/github-first-ci.json. Native verification is remote Ubuntu only, not a local native execution.

A source release is created only after the final documentation snapshot CI passes. Its exact final SHA, release URL and later raw commit count are verified at delivery, outside this self-referential evidence commit. Application contacts are outside Git and require participant confirmation. MoonCakes publication has not occurred; initial-review acceptance is not claimed.
